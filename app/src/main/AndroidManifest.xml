<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hector.myapplication">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_true"
        android:label="X-pense Tracker"
        android:roundIcon="@mipmap/ic_launcher_true_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="X-pense Tracker">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login" />
        <activity
            android:name=".SharedExpensesActivity"
            android:label="@string/title_activity_shared_expenses" />
        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings" />
        <activity
            android:name=".SummariesActivity"
            android:label="@string/title_activity_summaries" />
        <activity
            android:name=".ExpensesActivity"
            android:label="@string/title_activity_expenses" />
        <activity android:name=".SettingsAboutUsActivity" />
        <activity android:name=".SettingsSortingPreferenceActivity"></activity>
    </application>

</manifest>